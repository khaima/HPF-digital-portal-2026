server.log
__pycache__/
.DS_Store
repo.zip
HPF-digital-portal-2026/

# Never commit secrets. This site is static — every tracked file is served to
# the browser, so a secret key here is a public secret. The project URL and
# publishable key are the only two that belong in config.js; the secret key
# and database password belong in neither.
.env
.env.*
!.env.example
