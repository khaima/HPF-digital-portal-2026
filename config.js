/* ============================================================
   Supabase connection settings.

   Both values are safe to publish: access is controlled by the
   row-level security policies in supabase/schema.sql, not by
   keeping the key secret. Never put the service_role key or the
   database password here — this file ships to every browser.
   ============================================================ */

export const SUPABASE_URL = "https://zptupvyrwoeabncxabgj.supabase.co";
export const SUPABASE_PUBLISHABLE_KEY = "sb_publishable_7TjjWMCk6Hrc-nut_qLMoQ_YnJM1C93";
